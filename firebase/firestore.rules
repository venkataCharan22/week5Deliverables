rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for development — lock down before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
